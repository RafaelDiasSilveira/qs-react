language: node_js

node_js:
  - "stable"

cache:
  yarn: true
  directories:
  - node_modules

before_install:
  - export PATH=$HOME/.yarn/bin:$PATH
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.7.0

script:
  - yarn install
  - yarn run build:prod
